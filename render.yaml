services:
  - type: web
    name: blog
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: OWNER_TOKEN
        value: dev-token
      - key: DATABASE_URL
        sync: false
